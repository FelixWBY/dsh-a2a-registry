# Copy to /etc/dsh/registry-edge.env, replace both values, and grant read
# access only to root and the Caddy service group. This file contains no
# Registry, Harness, device, database, admission, or alert credential.

REGISTRY_DOMAIN=
ACME_EMAIL=
